coverage:
  status:
    project:
      default:
        target: 100%
        threshold: 1%

comment:
  layout: "condensed_header, diff, flags, components"

component_management:
  individual_components:
    - component_id: main  # this is an identifier that should not be changed
      name: crxst  # this is a display name, and can be changed freely
      paths:
        - src/main.rs
